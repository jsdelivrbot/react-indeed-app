import React , { Component } from 'react'


const GraphComponent = (props) => {
  return(
    <div className="container">
      <h2 className="graph-msg">Job offers history is not available for current job</h2>
    </div>
  )

}

export default GraphComponent
